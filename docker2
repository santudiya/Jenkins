pipeline {
  agent {
    docker { image 'jenkins:slave' }
  }
 environment {
                var1 = "test_password"
            }

    stages {
        stage('My Environment Pipeline') {
            
            steps {
                sh "echo 'this is my first environment $var1'"
            }
        }
    }
    }
